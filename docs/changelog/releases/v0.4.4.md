# Macrion 0.4.4

## Bug Fixes & Refinements

- Technical preparations (neutralized OpenCV build metadata, aligned JDK runtime, deterministic packaging, and uncompressed model assets) to achieve byte-for-byte reproducible builds for F-Droid; this release contains no user-facing changes and existing users do not need to update.
